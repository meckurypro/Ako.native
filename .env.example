# Copy this file to .env and fill in your real values (never commit .env itself)
# Same two values as the web app's VITE_ vars — just renamed with the
# EXPO_PUBLIC_ prefix Expo requires to expose them to the app bundle.
EXPO_PUBLIC_SUPABASE_URL=https://nokuwkxosyegaexlzdsl.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
