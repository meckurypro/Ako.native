// File: app/(auth)/login.tsx
// Expo Router convention: this file's job is just to be the route.
// The actual UI lives in screens/LoginScreen.tsx, kept separate so
// screen logic isn't tangled with routing/file-based-route concerns.
export { LoginScreen as default } from "../../screens/LoginScreen";
